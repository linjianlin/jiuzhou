/**
 * 安全相关应用模块集合。
 *
 * 作用：
 * 1. 做什么：集中承载登录、兑换码等跨业务复用的安全控制能力。
 * 2. 做什么：为上层服务提供统一入口，避免安全规则散落在具体业务目录。
 * 3. 不做什么：不直接承接 HTTP 协议，不替代具体业务服务。
 *
 * 输入 / 输出：
 * - 输入：由各业务服务按需引入具体安全能力。
 * - 输出：复用型安全模块。
 *
 * 数据流 / 状态流：
 * - 应用服务 -> 本目录下的安全模块 -> Redis / 共享状态。
 *
 * 复用设计说明：
 * - 登录与兑换码都需要失败计数与短时锁定，这里作为单一入口后，后续新增敏感操作无需复制底层实现。
 *
 * 关键边界条件与坑点：
 * 1. 安全模块只提供规则能力，业务文案和副作用仍由调用方决定。
 * 2. 新增安全规则时必须先复用现有键结构，避免和已上线 Redis 协议漂移。
 */
pub mod attempt_guard;
