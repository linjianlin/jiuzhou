/**
 * 兑换码应用模块集合。
 *
 * 作用：
 * 1. 做什么：集中管理兑换码相关的应用服务。
 * 2. 做什么：为 HTTP 路由与后续建码链路提供统一业务入口。
 * 3. 不做什么：不承载路由协议，不直接暴露数据库细节给外层。
 *
 * 输入 / 输出：
 * - 输入：由边界层按需引入兑换码服务。
 * - 输出：兑换码业务模块。
 *
 * 数据流 / 状态流：
 * - 路由或 webhook 入口 -> 本目录服务 -> 数据库 / Redis。
 *
 * 复用设计说明：
 * - 兑换、建码、来源幂等等后续都归到同一目录，避免爱发电链路和游戏内兑换入口各写一套服务。
 *
 * 关键边界条件与坑点：
 * 1. 该目录只放兑换码应用层，不把邮件、奖励等通用能力反向塞回来。
 * 2. 对外协议必须继续由边界层定义，避免服务层耦合 HTTP shape。
 */
pub mod service;
